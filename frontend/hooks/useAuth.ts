"use client";
export default function useAuth() {
    // auth logic host
    // auth logic participant

    const hostId = 1;
    const hostUsername = "vermayash@gmail.com";
    const participantId = 1;

    return { hostId, hostUsername, participantId };
}
